# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{public/.+\.(css|js|html)})
end

guard 'coffeescript', :input => 'public/coffee', :output => 'public/app'
